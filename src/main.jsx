import React from 'react'
import ReactDOM from 'react-dom/client'
import RoutesWeb from './Routes.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RoutesWeb />
  </React.StrictMode>,
)
